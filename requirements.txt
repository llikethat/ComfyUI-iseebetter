# ComfyUI-iSeeBetter Requirements
# Core dependencies (usually already installed with ComfyUI)
torch>=1.10.0
torchvision>=0.11.0
numpy>=1.19.0

# Optical flow computation
opencv-python>=4.5.0

# Optional: For advanced optical flow methods
# These are not required but can improve quality
# kornia>=0.6.0

# Note: The pyflow library used in the original iSeeBetter
# requires compilation. If you want to use it:
# 1. Clone: git clone https://github.com/pathak22/pyflow
# 2. Build: cd pyflow && python setup.py build_ext -i
# 3. Copy pyflow*.so to this directory
